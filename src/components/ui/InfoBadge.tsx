import { Info } from 'lucide-react';

export const InfoBadge = () => (
  <span className="inline-flex items-center justify-center w-4 h-4 rounded-full border border-gray-300 mr-1">
    <Info className="w-[10px] h-[10px]" strokeWidth={1.5} />
  </span>
);